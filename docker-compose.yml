services:
  stream247:
    build: .
    container_name: stream247-panel
    restart: unless-stopped
    environment:
      - TZ=Asia/Baghdad
      - DATA_DIR=/data
      # Optional Basic Auth:
      # - ADMIN_USER=admin
      # - ADMIN_PASSWORD=change-me-now
    volumes:
      - ./data:/data
    ports:
      - "28081:8080"
